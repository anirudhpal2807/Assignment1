// Input field variants and sizes
export enum InputVariant {
  FILLED = 'filled',
  OUTLINED = 'outlined', 
  GHOST = 'ghost'
}

export enum InputSize {
  SMALL = 'sm',
  MEDIUM = 'md',
  LARGE = 'lg'
}

// Data table selection modes
export enum SelectionMode {
  SINGLE = 'single',
  MULTIPLE = 'multiple'
}